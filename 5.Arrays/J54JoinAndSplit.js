// Join : This is used to convert an array into a string
let numbers = [1,2,3,45,6,9,7];
console.log(numbers.join()); //Default: ',' is used to join
console.log(numbers.join('-'));

// Split: This is used to convert a string into an array
let text = 'Hi I am akhil';
let textToArray = text.split(' ');
console.log(textToArray);